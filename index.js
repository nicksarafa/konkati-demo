import { AppRegistry } from 'react-native';
import App from './App';

/**
 * @desc import Expo for use with the command line tool
 * @see https://docs.expo.io/versions/latest/index.html
 */
import Expo from 'expo';

AppRegistry.registerComponent('KontakiDemo', () => App);
